let a = Number(prompt('Primeiro um número: '))
let b = Number(prompt('Segundo número'))
let c = Number(prompt('Terceiro número: '))

if ((a+b) > c ) {
    console.log(`A soma de A (${a}) e de B (${b}) é maior que C (${c})`)
    
} else {
    
}